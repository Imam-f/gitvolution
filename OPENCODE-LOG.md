# OPENCODE Log

Things told to opencode in this folder (`gitvolution`), collected from the local session history.

## 2026-09-07

1. Make an electron app here that I can select a git repo, then it select one file, then it has a time slider to see the evolution of the file. There are 3 panels: middle = current commit, right = next commit, left = previous commit.

2. Make sure it can collapse and only show the changes, and can jump so it only shows the changes.
3. Make it pad the diff so all 3 are aligned.


4. Make next and prev change jump to next change (not per line), also make the color flat instead of tinted, also add recent repo.
5. Can you commit per changes?
6. Make 2 view modes, also preload and cache the commit or something so it moves between commits faster.


7. Add recent repo and recent file.
8.  Make the view aligned, add padding if necessary.

9.  Make next and prev change jump to next change (not per line), also make the color flat instead of tinted.



10. Can you open opencode and collect what prompt I ask in this folder? (… figure it out where)
11. Use opencode CLI, then make a markdown list of things I tell to opencode in this folder.
12. There are more.
